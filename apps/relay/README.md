# Relay and executor

Planned stateless orchestration for request dispatch, FCC result collection,
FDC checkpoint resume, and permissionless execution. It receives no policy
decryption capability and cannot supply or override authorization.
